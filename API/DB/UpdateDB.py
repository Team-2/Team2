from Start_DB import Records , Days , session

def get_pd_data(sheep_name: int, record_day: int, csv_path: str):

    pass


def get_result() -> float:

    # todo Get result from NN
    pass


def update_db_data(sheep_name: int, record_day: int):
    # todo Update DB by NN and cvc file
    # for i in get_pd_data():
    #     temp = Records(...)
    # temp_ship = Ships(sheep_name=sheep_name, record_day=record_day,
    #                   forecast=get_result())

    # temp_ship.records = temp
    #
    # session.add_all(temp_ship)
    # session.commit()
    pass
